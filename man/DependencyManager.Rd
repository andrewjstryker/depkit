% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependency-manager.R
\name{DependencyManager}
\alias{DependencyManager}
\title{Construct a DependencyManager}
\usage{
DependencyManager(
  registry = list(),
  output_root = NULL,
  url_root = NULL,
  cdn_mode = "off"
)
}
\arguments{
\item{registry}{Optional list of html_dependency objects to pre-register.}

\item{output_root}{Filesystem root for copied assets.}

\item{url_root}{Base URL for emitted assets.}

\item{cdn_mode}{CDN handling mode (\code{"off"} or \code{"verify"}).}
}
\value{
A \code{dependency_manager} object.
}
\description{
Creates a new dependency manager that tracks HTML dependencies, deduplicates
assets, copies files to an output directory, and emits deterministic HTML
tags. Use \code{\link[=insert]{insert()}} to register dependencies and \code{\link[=emit_css]{emit_css()}}/\code{\link[=emit_js]{emit_js()}}
to generate the corresponding HTML.
}
\examples{
dm <- DependencyManager(
  output_root = tempfile("assets"),
  url_root = "/static"
)
dep <- htmltools::htmlDependency(
  "example", "1.0",
  src = c(file = system.file(package = "htmltools")),
  script = NULL
)
upd <- insert(dm, dep)
dm(upd)

}
\seealso{
\code{\link[=insert]{insert()}}, \code{\link[=has]{has()}}, \code{\link[=remove]{remove()}}, \code{\link[=emit_css]{emit_css()}}, \code{\link[=emit_js]{emit_js()}}
}
